#include <void_generics/nkl_hashmap.h>

NklHashmap nkl_hashmap_create(size_t capacity, size_t value_size, size_t (*hash_fn)(void *key)) {
    NklHashmap m = {
        .value_size = value_size,
    };

    m

    return m;
}
